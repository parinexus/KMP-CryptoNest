package parinexus.kmp.first.testing

import androidx.activity.ComponentActivity

/**
 * Empty activity host for Compose UI instrumented tests.
 * Declared in the debug manifest so it runs in the app process.
 */
class ComposeHostActivity : ComponentActivity()
